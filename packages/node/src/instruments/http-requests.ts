// This module is intentionally empty — HTTP request tracking
// is done via the Express/Fastify integrations which have access
// to the req/res objects directly. This file exists for the
// index barrel export pattern.
export {};
