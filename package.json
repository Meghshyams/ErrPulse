{
  "name": "errpulse-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r --filter @errpulse/core build && pnpm -r --filter !@errpulse/core --filter !errpulse-dashboard build",
    "build:core": "pnpm -r --filter @errpulse/core build",
    "build:server": "pnpm -r --filter @errpulse/server build",
    "build:node": "pnpm -r --filter @errpulse/node build",
    "build:react": "pnpm -r --filter @errpulse/react build",
    "build:cli": "pnpm -r --filter errpulse build",
    "build:dashboard": "pnpm -r --filter errpulse-dashboard build",
    "dev:dashboard": "pnpm -r --filter errpulse-dashboard dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "pnpm -r exec rm -rf dist node_modules",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,css,md,yml,yaml}": "prettier --write"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.1",
    "prettier": "^3.8.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
